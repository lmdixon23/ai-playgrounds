# Metrics workspace

Keep raw analytics exports private. Commit only definitions, aggregate publication tables, and synthetic templates unless a disclosure review confirms that low-count geography and referrer fields are safe. Use `scripts/snapshot_public_metrics.py` for GitHub metadata and export GoatCounter aggregates on the same monthly boundary.
