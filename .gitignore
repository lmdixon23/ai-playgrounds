# Local-only notes & scratch — never published.
# Keeps _local/ (CLAUDE.md, results.json, etc.) off GitHub and off the live site.
_local/

# Eval-harness output (regenerated on demand via the Download button)
tests/results*.json
results*.json

# OS / editor cruft
.DS_Store
.AppleDouble
Thumbs.db
Desktop.ini
.vscode/
.idea/
.vs/
*~
*.swp
*.swo
*.bak
*.orig
