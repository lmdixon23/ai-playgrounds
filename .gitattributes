# Canonical source and generated-public hashes are byte-exact. Keep every text
# checkout LF-stable on Windows as well as Unix; let Git continue to classify
# binary files automatically.
* text=auto eol=lf
